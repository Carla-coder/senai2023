#include <stdio.h>
int main(){
		int numero;
		printf("Informe um numero\n");
		scanf("%d , &numero");
		if (numero > 0){
			printf("O numero %d é > 1", numero);
	} else {
		printf("O numero %d é < 1", numero);
	}
return 0;
	
}
